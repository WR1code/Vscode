打开 VS Code 的设置 (Settings)。

搜索 Proxy，找到 Http: Proxy 选项。

填入你本地代理的本地监听地址（例如 [http://127.0.0.1:7890](http://127.0.0.1:7890)，具体端口取决于你的代理软件）。

确保 Http: Proxy Strict SSL 是关闭（取消勾选）状态。

重启 VS Code 后再尝试登录。



将代理切换到tun模式
